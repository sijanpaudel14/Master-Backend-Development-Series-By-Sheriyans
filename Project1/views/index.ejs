<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>

  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="w-full rounded-md outline-none block px-5 py-2 bg-zinc-800"
            type="text"
            placeholder="Title goes here.."
            name="title"
          />
          <textarea
            class="w-full rounded-md outline-none mt-2 block px-5 py-2 resize-none bg-zinc-800"
            placeholder="Write your task details here..."
            name="description"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
            value="Add Task"
          />
        </form>
      </div>

      <div class="tasks p-10 text-white flex flex-wrap gap-3">
        <% if (files.length>0){ %> <% files.forEach(function(val){ %>

        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-3xl tracking-tighter"><%= val %></h1>
          <div class="w-full flex justify-between items-center mt-5">
              <a href="/file/<%= val %> " class="text-blue-600 inline-block">Read more...</a>
              <a class="text-zinc-600" href="/edit/<%= val %>">Edit</a>
            </div>
        </div>

        <% }); %> <% } else { %>
        <h3 class="text-zinc-600">No task added yet.</h3>

        <% }%>
      </div>
    </div>
  </body>
</html>
