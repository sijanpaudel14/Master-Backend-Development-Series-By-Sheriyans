<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="text-xl"> 
            <a class="inline-block bg-green-500 text-black px-3 py-2 rounded-md mb-10" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Create User</h1>
        
        <form action="/create"method="post" class="flex gap-6 flex-wrap flex-col justify-center w-1/3">
            <input type="text" required class="px-5 py-2 border-2 border-zinc-800 rounded-md  bg-transparent outline-none" name="name" placeholder="name">
            <input type="text" class="px-5 py-2 border-2 border-zinc-800 rounded-md  bg-transparent outline-none" name="email" placeholder="email">
            <input type="text" class="px-5 py-2 border-2 border-zinc-800 rounded-md bg-transparent outline-none" name="image" placeholder="image url">
            <input type="submit" class="ml-20 px-5 py-2 w-1/3 bg-blue-500 rounded-lg cursor-pointer" value="Create User">
        </form>
    </div>
    <div id="toast" class="fixed top-5 left-1/2 bg-green-500 text-white px-4 py-2 rounded-lg shadow-lg opacity-0 transform translate-y-8 transition-all duration-300 ease-in-out">
        User created successfully!
    </div>
        <script>
    // Check if the URL contains ?success=true
    const urlParams = new URLSearchParams(window.location.search);
    const success = urlParams.get('success');

    if (success === 'true') {
        const toast = document.getElementById('toast');
        toast.classList.remove('opacity-0', 'translate-y-8');
        toast.classList.add('opacity-100', 'translate-y-0');

        // Automatically hide the toast after 3 seconds
        setTimeout(() => {
            toast.classList.remove('opacity-100', 'translate-y-0');
            toast.classList.add('opacity-0', 'translate-y-8');
        }, 3000);
    }
</script>

    </script>
</body>
</html>