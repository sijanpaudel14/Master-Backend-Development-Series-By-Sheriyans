!<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-10">
      <h5 class="mb-5">Edit your post.</h5>

      <!-- Post Form -->
      <form method="post" action="/update/<%= post._id %>">
        <textarea
          class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block"
          placeholder="What's on you mind?"
          name="content"
          value=""
          id=""
        >
        <%= post.content %>
    </textarea>
        <input
          type="submit"
          value="Update your post"
          class=" px-3 w-40 py-2 bg-yellow-500  text-black block rounded-md mt-2 cursor-pointer"
        />
      </form>
    </div>
  </body>
</html>
